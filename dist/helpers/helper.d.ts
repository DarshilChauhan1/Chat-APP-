export declare class ResponseBody {
    statusCode: number;
    message: string;
    data: Object;
    constructor(statusCode?: number, message?: string, data?: Object);
}
export declare const otherMember: (members: any, userId: string) => any;
